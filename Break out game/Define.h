#pragma once // Prevents duplicate header files when there are many files,
// which can cause errors. You can just leave it as is.

#include <iostream>
#include <Windows.h>
#include <time.h>
#include <conio.h>
using namespace std;

struct INFO // Ball
{
	int m_nX;
	int m_nY;
};
